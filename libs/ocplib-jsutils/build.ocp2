OCaml.library("ocplib-jsutils",
  ocaml + {
  files = [
    "js_utils.ml",   { pp = camlp4_js };
    "lwt_request.ml", { pp = camlp4_js };
  ];
  requires = [
    "lwt";
    "js_of_ocaml.tyxml";
  ];
  });

OCaml.library("ocplib-xor",
  ocaml + {
  has_asm = true;
  files = [ "xor.ml" ];
  requires = [ "base64" ];
  });
